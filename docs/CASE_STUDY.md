# 具體成果與驗證範圍

## 1. 完整系統曾跑通的操作流程

2026-09-12，版本 `9012a79` 在隔離 PostgreSQL、實際網頁與三個不同登入帳號中完成以下**合成資料測試**。下列畫面和 CSV 都取自當次執行，保留原始內容。畫面中的 AAAS-TW 對應同一個帳跡 LedgerTrail 專案，現持續作為工程名使用；截圖仍屬上述歷史測試版本。

| 操作 | 實際結果 | 這項結果說明什麼 |
|---|---|---|
| 建立 105 元草稿，再退回補件 | 舊案為 `needs_information`，保留來源及覆核紀錄 | 可以追查退回原因與原始版本 |
| 補件為 210 元的新版本 | 產生新的草稿識別；仍須重新認列、確認與覆核 | 修改資料後不能直接沿用舊核准 |
| 獨立核准後，由第三個帳號過帳 | 新版為 `posted`，資料庫產生分錄 | 網頁、權限、服務與資料庫接成可操作路徑 |
| 拒絕一次對帳提案，再確認新提案 | 保留拒絕紀錄；新配對為 `confirmed` | 對帳有明確的提案與獨立確認 |
| 依對應佐證結案例外，再重開處理 | 留下調查、結案、重開與再次結案的歷程 | 已處理案件仍能追查及重新處理 |
| 產生並下載 CSV，記錄獨立交付 | 同一筆分錄的 3 行明細，`delivery_status=delivered` | 系統能交付實際檔案，並分開記錄交付狀態 |

當次瀏覽器結果為 `PASS`，保存 19 個命令回應，`page_errors=[]`；另檢查登入切換、操作中切頁、回應遺失後重試與例外重開。這些是特定版本的工程成果，沒有實際客戶、工時節省或收件方採用的證明。

**退回補件：105 元舊版與覆核紀錄**

![105 元草稿退回補件的原始測試畫面](../assets/workbench-returned.png)

**新版過帳：210 元、分錄與前後版本歷史**

![210 元新版過帳的原始測試畫面](../assets/workbench-posted.png)

畫面中尚未完成的範圍選擇等標記照實保留；這是當時的測試工作台。

### 實際匯出結果

可下載[當次原始合成 CSV](../evidence/synthetic-accounting-export.csv)。下表依原檔金額整理：

| 分錄用途 | 借方 | 貸方 |
|---|---:|---:|
| 銷貨應收款 | 210.00 | 0.00 |
| 銷項稅額 | 0.00 | 10.00 |
| 銷貨收入 | 0.00 | 200.00 |

交付紀錄與收件方接受分開處理：當次 `recipient_acceptance=not_established`，未宣稱會計師或客戶已接受。

## 2. 讀者可以自行重跑的程式實作

本庫完整公開[政策模組](../src/domain/accounting/posting_policy.py)與[配套測試](../tests/test_posting_policy.py)，讓讀者檢查資料結構、金額處理、拒絕條件與測試設計。

獨立模組範例使用未稅 101 元、稅額 5 元、總額 106 元；通過條件時產生 106／101／5 的分錄建議。這與上面的 210 元完整系統案例是不同測試。

| 改變輸入 | 模組結果 |
|---|---|
| 缺少來源識別或必要驗證狀態 | 不產生分錄，要求補足來源 |
| 應收帳款被對應為負債、正常貸方 | 不產生分錄，回報科目語意不符 |
| 總額改為 106.01 元 | 不產生分錄，回報金額不一致 |
| 組織政策建立者與核准者相同 | 不產生分錄，回報核准條件不符 |

模組使用 `Decimal` 處理金額；它信任呼叫端提供的來源與核准狀態，不自行驗證原始文件、認證身分或寫入帳簿。成功結果為 `proposed`，`posting_allowed=True` 只表示通過這個函式的判定。組織政策要求 `synthetic_test_fixture_only_not_professional`，專業政策適用性仍未核定。

## 3. 哪些已驗證，哪些仍待補驗

| 驗證範圍 | 實際結果 | 可支持的結論 |
|---|---|---|
| 上述完整系統歷史版本 `9012a79` | 19 個工作中 18 個成功；會計資料庫 49 項通過，三人瀏覽器流程通過；安全套件 24 項通過、2 項失敗 | 上述操作成果成立；整輪驗收尚未通過 |
| 後續完整系統來源 | 最後兩項資料庫權限規則已修補，最新完整回歸未完成 | 修復存在，仍須補驗，不沿用舊版成功結果 |
| 本庫公開政策模組 | 獨立 Python 3.12.14／pytest 9.0.2，本地 112 項通過、0 跳過 | 可重跑的模組行為；不代表完整系統或真實會計適用性 |

上述完整系統結果來自既有測試產物；品牌改名不會把它們變成新版整體驗收。公開程式、原圖與 CSV 仍以雜湊核對原版本；來源、產物識別與結果見 [RESULTS.json](../evidence/RESULTS.json)。

原始完整測試資料保留於私人專案；本庫摘要由 AI 核對整理，並非獨立第三方驗證。[完整功能與待辦](ARCHITECTURE.md) · [我的角色與 AI 分工](../README.md#我的角色與-ai-分工)
