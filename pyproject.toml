[project]
name = "ledgertrail-portfolio"
version = "0.3.0"
description = "LedgerTrail accounting policy implementation for portfolio evaluation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
